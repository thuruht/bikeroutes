node_modules/
dist/
.wrangler/
.dev.vars
*.local
.DS_Store

# Local Cloudflare reference docs (copied from external repo)
docs/cloudflare/

# Vite build output (generated into worker dir for Workers static assets)
worker/public/

.dev.vars*
!.dev.vars.example
.env*
!.env.example

# Local routing container build (Valhalla Docker) — ignore build outputs, keep the source Dockerfile
routing/**
!routing/valhalla/
!routing/valhalla/Dockerfile.valhalla
routing/valhalla/valhalla.json
routing/valhalla/tiles.tar

# Data pipeline build/cache artifacts
data-pipeline/.build/
data-pipeline/.cache/

# Baked Valhalla tile context generated during CI (keep the Dockerfile)
worker/valhalla/valhalla.json
worker/valhalla/tiles.tar

