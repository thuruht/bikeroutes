{
  "name": "bikeroutes",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "upload-version": "cd worker && npm ci && npx wrangler versions upload"
  },
  "devDependencies": {
    "wrangler": "^4.127.0"
  }
}
