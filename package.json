{
  "name": "bikeroutes",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd ../worker && npm ci",
    "deploy": "cd worker && npm ci && npm run deploy",
    "upload-version": "cd worker && npm ci && npx wrangler versions upload"
  }
}
